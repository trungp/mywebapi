apiVersion: v1
kind: Service
metadata:
  name: mywebapi
  namespace: ${DEV_SPACE}
  labels:
    app: mywebapi
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: mywebapi
